# trading-guide-prompt-chain
A two-step guide generator using prompt chaining with Gemini API. It first creates a structured plan in JSON format, then executes it to produce a detailed guide in Markdown.
